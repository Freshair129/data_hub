# คู่มือการใช้งานระบบดักจับชื่อ Agent อัตโนมัติ (Automated Agent Sync) 🤖🚀

สคริปต์นี้จะช่วยให้ CRM ของเราฉลาดเท่า Facebook Business Suite โดยการใช้ "หุ่นยนต์เบราเซอร์" เข้าไปอ่านชื่อคนตอบแชทให้เองครับ

## สิ่งที่ต้องเตรียม (ทำครั้งเดียว)
เปิด Terminal ในโฟลเดอร์ `crm-app` แล้วรันคำสั่งนี้ครับ:

```bash
npm install playwright
npx playwright install chromium
```

## วิธีการเริ่มทำงาน
รันคำสั่งนี้เพื่อเริ่มให้หุ่นยนต์ทำงานครับ:

```bash
node automation/sync_agents.js
```

## การทำงานของระบบ
1.  **การล็อคอิน**: เมื่อรันครั้งแรก จะมีหน้าต่างเบราเซอร์เปิดขึ้นมา ให้ Boss ล็อคอิน Facebook ให้เรียบร้อยครับ (ระบบจะจำไว้ ไม่ต้องล็อคอินซ้ำ)
2.  **การอ่านข้อมูล**: หุ่นยนต์จะเข้าไปที่หน้า Inbox และไล่ดูแชทล่าสุด 20 คน เพื่อหาประโยคที่ว่า "ส่งโดย [ชื่อพนักงาน]"
3.  **การอัปเดต**: เมื่อเจอชื่อ (เช่น Fah, Joy) หุ่นยนต์จะส่งข้อมูลนั้นกลับมาที่ CRM ของเราทันทีครับ

---

> [!TIP]
> **เทคนิคการรันตลอดยี่สิบสี่ชั่วโมง:**
> Boss สามารถตั้งรันสคริปต์นี้ทิ้งไว้ในเครื่อง Mac ได้เลยครับ โดยการแก้โค้ดบรรทัดสุดท้ายจาก `syncAgents();` เป็น:
> ```javascript
> setInterval(syncAgents, 60 * 60 * 1000); // ทำงานทุกๆ 1 ชั่วโมง
> syncAgents();
> ```
> แล้วรันคำสั่ง `node automation/sync_agents.js` ทิ้งไว้ครับ!

---

## สถาปัตยกรรมและการจัดการ ID (ID Mapping) 🆔

เพื่อให้ระบบ CRM และ Automation ทำงานร่วมกันได้อย่างถูกต้อง เมื่อมีการเพิ่มพนักงานใหม่หรือเปลี่ยนชื่อใน Facebook เจ้าหน้าที่ดูแลระบบต้องอัปเดตไฟล์:
`docs/id-mapping.yaml`

### สิ่งที่ต้องทำเมื่อมีการเปลี่ยนแปลง:
1.  **พนักงานใหม่**: ตรวจสอบว่าชื่อใน Facebook ของพนักงาน (Facebook Name) ตรงกับข้อมูลใน `Employee.facebookName`
2.  **ID Mapping**: บันทึกการเชื่อมโยง ID ใหม่ๆ (เช่น Page ID หรือ ID เฉพาะ) ลงในไฟล์ `id-mapping.yaml` เพื่อให้เป็น Single Source of Truth
3.  **การระบุตัวตน (Attribution)**: 
    - `Message.responderId` จะถูกผูกกับ `Employee.id` โดยใช้ชื่อจากตัว Scraper นี้
    - หากระบบไม่สามารถระบุตัวตนได้ (unresolved) ให้ตรวจสอบ Alias ใน `Employee.metadata`
